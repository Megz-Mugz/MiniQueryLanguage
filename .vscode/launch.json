{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Build and Run",
        "type": "lldb",                        
        "request": "launch",
        "program": "${workspaceFolder}/src/main",   
        "cwd": "${workspaceFolder}",                
        "preLaunchTask": "build program"           
      }
    ]
  }